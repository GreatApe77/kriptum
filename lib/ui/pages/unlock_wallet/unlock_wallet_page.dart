import 'package:flutter/material.dart';

class UnlockWalletPage extends StatelessWidget {
  const UnlockWalletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Unlock Wallet Page'),
      ),
    );
  }
}
