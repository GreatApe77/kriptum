import 'package:flutter/material.dart';

class ChooseAmountWidget extends StatelessWidget {
  const ChooseAmountWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: ElevatedButton(onPressed: () {}, child: Text('boltar')),
      ),
    );
  }
}
